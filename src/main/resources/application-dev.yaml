spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/todolist_dev
    username: todouser
    password: todopass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
